# ResumeApp
resume for Bryson made w/ Kaeden
readmereadme

Hosted on Vercel (deploys automatically from `main`).

- `index.html`: the one-page site (the contact QR code is a static SVG, so it works offline)
- `bryson-states.vcf`: downloadable contact card ("Save contact" link)
- `resume.pdf`: linked from the "View my resume" button (to be added)
